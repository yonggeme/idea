<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.idea.dao.SeckillDao">

    <sql id="allCloumns">id,name,number,start_time,end_time,create_time</sql>

    <select id="queryAll" resultType="Seckill">
        SELECT <include refid="allCloumns"/> FROM  seckill limit #{offset},#{limit};
    </select>

    <select id="queryById" resultType="queryById">
        SELECT <include refid="allColumns"/> FROM seckill WHERE id = #{seckillId};
    </select>

    <update id="reduceNumber">
        UPDATE
          seckill
        SET
          number = number -1
        WHERE seckill_id = #{seckillId}
          AND start_time <![CDATA[ <= ]]> #{seckillDateTime}
          AND end_time >= #{seckillDateTime}
          AND number > 0;
    </update>
 </mapper>